{
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "start": "node .output/server/index.mjs",
    "generate": "nuxt generate",
    "postgenerate": "mkdir -p ./public_html && rsync -a --delete ./.output/public/ ./public_html/"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "nuxt": "^4.0.3"
  },
  "devDependencies": {
    "sass": "^1.90.0",
    "typescript": "^5.9.2"
  }
}
